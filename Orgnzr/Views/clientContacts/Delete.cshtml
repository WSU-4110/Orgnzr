@model Orgnzr.Models.ClientContact

@{
    ViewData["Title"] = "Delete Contact";
}

<h1>Delete Contact</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Client Contact</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            First Name
            <!--(At symbol)Html.DisplayNameFor(model => model.firstName) -->
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.firstName)
        </dd>
        <dt class="col-sm-2">
            Last Name
            <!--(At symbol)Html.DisplayNameFor(model => model.lastName) -->
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.lastName)
        </dd>
        <dt class="col-sm-2">
            Phone Number
            <!--(At symbol)Html.DisplayNameFor(model => model.phoneNumber) -->
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.phoneNumber)
        </dd>
        <dt class="col-sm-2">
            Email Address
            <!--(At symbol)Html.DisplayNameFor(model => model.emailAddress) -->
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.emailAddress)
        </dd>
        <dt class="col-sm-2">
            Preferred Contact
            <!--(At symbol)Html.DisplayNameFor(model => model.prefferredContact) -->
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.preferredContact)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="clientId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
